function greet(name: string): string {
    return `Hello, ${name}!`;
}


// testing on terminal when you run "node dist/codewars_scripts/example.js":
const message = greet('Codewars developer');
console.log(message);
